// Identificar os dois menores numeros positivos em um array
// Somar esses dois numeros
// Retornar a soma como resultado


// ↓ Pseudo-Codigo ↓

// Receber o array de numeros positivos
// Ordernar a array para garantir que os menores numeros fiquem nas primeiras posicoes
// Selecionar os dois primeiros numeros (os menores apos a ordenacao)
// Somar esses dois numeros
// Retornar o resultado

function desafio(array) {
  if (array.every(num => typeof num === `number` && num > 0)){

    

  } else {return `nao positivo`}
}