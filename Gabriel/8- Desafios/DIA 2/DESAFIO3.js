/* 
3. Concatenando strings
   Crie duas variáveis com partes de uma frase. Concatene-as em uma terceira variável e exiba o
   resultado no console.
*/

const nome = `Gabriel`;
const sobrenome = ` Politano`;
const nomeCompleto = nome + sobrenome;

console.log(nomeCompleto);