/* 
9. Operador ternário
   Declare uma variável idade e use um operador ternário para verificar se a pessoa é maior de
   idade. Exiba 'Maior de idade' ou 'Menor de idade' conforme o valor.
*/

const idade = 18;
const maior = (idade >= 18) ? `maior` : `menor`;

console.log(maior);