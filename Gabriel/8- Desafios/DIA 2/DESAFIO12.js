/* 
12. Conversão de tipos
    Converta uma string para número usando Number(). Depois, converta um número para string
    usando String(). Exiba ambos os resultados e seus tipos.
*/

const str = `3`;
const num = 1;

console.log(typeof(Number(str)));

console.log(typeof(String(num)));
