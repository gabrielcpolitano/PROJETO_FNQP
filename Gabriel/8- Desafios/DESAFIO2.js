/* 
2. Tipos de dados
   Declare variáveis usando os seguintes tipos de dados: string, number, boolean, null e undefined.
   Exiba os tipos no console usando typeof.
*/

const nome = `Gabriel`;
const verdadeiro = true;
const nulo = null;
const vazio = undefined;

console.log(typeof(nome));
console.log(typeof(verdadeiro));
console.log(typeof(nulo));
console.log(typeof(vazio));