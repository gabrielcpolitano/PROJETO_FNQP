/* 
17. Operações com NaN
    Declare uma variável contendo uma operação que resulte em NaN. Verifique o tipo e explique
    por que isso acontece.
*/

let num = `oi` / 5;

console.log(num);
console.log(`NaN pois nao e uma conta que seja possivel de ser realizada`)